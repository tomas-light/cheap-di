{
  "workspaces": [
    "lib",
    "ts-loader",
    "tests/*"
  ],
  "scripts": {
    "build:lib": "npm run build --workspace cheap-di",
    "build:ts-loader": "npm run build --workspace cheap-di-ts-transform",
    "build": "npm-run-all build:lib build:ts-loader",
    "test": "npm run webpack:dev --workspace @tests/webpack-project"
  },
  "dependencies": {
    "typescript": "5.2.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "6.9.1",
    "@typescript-eslint/parser": "6.9.1",
    "eslint": "8.53.0",
    "npm-run-all": "4.1.5",
    "prettier": "3.0.3"
  }
}
